vfork用于创建一个新进程，而该新进程的目的是exec一个新程序。
（1）vfork与fork都创建一个子进程，但vfork创建的子进程不将父进程的地址空间复制到子进程中，
     因为子进程会立即调用exec，于是不会存访问该地址空间。
（2）在子进程调用exec或exit之前，它在父进程的空间中运行，也就是说会更改父进程的数据段、栈和堆。
（3）vfork保证子进程先运行，在它调用exec或（exit）之后父进程才可能被调度运行；
     而fork中谁先运行取决于调度机的算法实现